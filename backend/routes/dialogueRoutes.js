/// ///////////////////////////////////////////////////
// INCLUDES
/// ///////////////////////////////////////////////////
const express = require('express')

const router = express.Router()

const controller = require("../controller/dialogueController")

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/<npcId>
/// ///////////////////////////////////////////////////
router.post("/:npcId", controller.insertDialogue)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/<dialogueId>
/// ///////////////////////////////////////////////////
router.put("/:dialogueId", controller.updateDialogue)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/<dialogueId>
/// ///////////////////////////////////////////////////
router.delete("/:dialogueId", controller.deleteDialogue)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/
/// ///////////////////////////////////////////////////
router.get("/", controller.getAllDialogue)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/<dialogueId>
/// ///////////////////////////////////////////////////
router.get("/:dialogueId", controller.getDialogueById)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/intro/<dialogueRowsToGet>
/// ///////////////////////////////////////////////////
router.get("/intro/:dialogueRowsToGet", controller.getDialogueIntro)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/display/<npcId>/<plotId>
/// ///////////////////////////////////////////////////
router.get("/display/:npcId/:plotId", controller.getFilteredDialogue)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/updateState/<dialogueId>
/// ///////////////////////////////////////////////////
router.put("/updateState/:dialogueId", controller.updateDialogueState)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/search/<keyword>
/// ///////////////////////////////////////////////////
router.get("/search/:searchInp", controller.searchDialogue)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/page/<pagination>
/// ///////////////////////////////////////////////////
router.get("/page/:pagination", controller.getAllDialoguesPagination)

/// ///////////////////////////////////////////////////
// Backend testing via Postman -> http://localhost:3000/api/dialogues/count/dialogues
/// ///////////////////////////////////////////////////
router.get("/count/dialogues", controller.countDialogues)

module.exports = router