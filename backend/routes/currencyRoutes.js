/// ///////////////////////////////////////////////////
// INCLUDES
/// ///////////////////////////////////////////////////
const express = require('express')

const router = express.Router()

const controller = require("../controller/currencyController.js")

/// ///////////////////////////////////////////////////
// To test, postman -> http://localhost:3000/api/currency/all/
/// ///////////////////////////////////////////////////
router.get("/all", controller.getAllCurrency)

/// ///////////////////////////////////////////////////
// To test, postman -> http://localhost:3000/api/currency/:userid/
/// ///////////////////////////////////////////////////
router.get("/:userid", controller.getCurrencyById)

/// ///////////////////////////////////////////////////
// To test, postman -> http://localhost:3000/api/currency/
/// ///////////////////////////////////////////////////
router.put("/:userid", controller.putCurrencyById)

/// ///////////////////////////////////////////////////
// To test, postman -> http://localhost:3000/api/currency/
/// ///////////////////////////////////////////////////
router.post("/:userid", controller.insertCurrency)

/// ///////////////////////////////////////////////////
// Backend URL  -> http://localhost:3000/api/currency/search/:username
/// ///////////////////////////////////////////////////
router.get("/search/:username", controller.getCurrencyByUsername)

module.exports = router