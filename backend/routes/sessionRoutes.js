/// ///////////////////////////////////////////////////
// INCLUDES
/// ///////////////////////////////////////////////////
const express = require('express')

const router = express.Router()

const controller = require("../controller/sessionController")

/// ///////////////////////////////////////////////////
// To test, postman -> http://localhost:3000/sessions/
/// ///////////////////////////////////////////////////
router.post("/", controller.insertSession)

/// ///////////////////////////////////////////////////
// To test, postman -> http://localhost:3000/sessions/<sessionId>
/// ///////////////////////////////////////////////////
router.get("/:sessionId", controller.getSession)


module.exports = router